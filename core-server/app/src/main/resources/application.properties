quarkus.banner.path=banner.txt
# ============================================================
# DATABASE
# ============================================================
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
quarkus.datasource.username=sa
quarkus.datasource.password=sa
# schema generation strategy
quarkus.hibernate-orm.schema-management.strategy=none
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true

# optional: see SQL in the logs
quarkus.hibernate-orm.log.sql=false
# ============================================================
# HTTP / CORS
# ============================================================
quarkus.http.root-path=/api
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=*
quarkus.http.cors.methods=GET,PATCH,PUT,POST,DELETE,OPTIONS
quarkus.http.cors.headers=*
quarkus.http.cors.exposed-headers=*
#quarkus.http.cors.access-control-allow-credentials=true
# ============================================================
# SECURITY / JWT
# ============================================================
# Where to find keys
mp.jwt.verify.publickey.location=../jwt/publicKey.pem
mp.jwt.verify.issuer=revoice-jwt
# Optional: if you want to sign JWTs from inside the app (e.g., /login endpoint)
smallrye.jwt.sign.key.location=../jwt/privateKey.pem
# Enforce auth (return 401 when no token instead of injecting dummy user)
quarkus.http.auth.proactive=true
# ============================================================
# LOGGING
# ============================================================
quarkus.log.level=INFO
# ============================================================
# REVOICECHAT CUSTOM CONFIG
# ============================================================
revoicechat.global.media-server-url=http://localhost:1
revoicechat.global.app-only-accessible-by-invitation=true
revoicechat.global.password.min-length=8
revoicechat.global.password.min-uppercase=1
revoicechat.global.password.min-lowercase=1
revoicechat.global.password.min-number=1
revoicechat.global.password.min-special-char=1
revoicechat.message.max-length=2000
revoicechat.dev.error.log=false
