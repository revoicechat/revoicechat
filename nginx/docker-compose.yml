services:
  nginx:
    image: nginx:alpine
    ports:
      - "${NGINX_PORT:-3000}:3000"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
networks:
  default:
    name: app-network